"""reliability — AI CAD 可靠性中间层 (MVP)

自然语言 → 需求澄清 → 结构化设计意图(DesignSpec) → 参数/约束校验
  → 可审查执行计划(Plan DAG) → 用户干预 → 分步执行 → 几何与语义验证
  → 可回滚修复

核心原则（需求文档 §1）：
1. LLM 不直接决定最终几何结果。
2. LLM 不直接执行未经审查的任意代码。
3. 重要设计意图必须先结构化。
4. 推断参数必须标明来源和置信度。
5. 用户确认的参数不能被 AI 静默覆盖。
6. 所有写操作都要能定位、验证和回滚。
"""

__version__ = "0.1.0"